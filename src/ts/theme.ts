import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#3fbd3a',
      contrastText: '#FFF',
    },
    text: {
      primary: '#000',
      secondary: '#9e9e9e',
    },
  },
});
